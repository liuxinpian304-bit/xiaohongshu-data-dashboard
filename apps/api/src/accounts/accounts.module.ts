import { Module } from '@nestjs/common'; import { AccountsController } from './accounts.controller'; import { AccountsService } from './accounts.service'; import { AuditService } from '../common/audit.service'; import { MockXhsConnector } from '@xhs/connector'; import { ConnectorRegistry } from './connector-registry';
@Module({ controllers: [AccountsController], providers: [AccountsService, AuditService, { provide: ConnectorRegistry, useFactory: () => new ConnectorRegistry([['mock', new MockXhsConnector()]]) }] }) export class AccountsModule {}
