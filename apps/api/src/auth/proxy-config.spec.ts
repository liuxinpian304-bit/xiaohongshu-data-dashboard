import { describe, expect, it } from 'vitest'; import { normalizeClientIp, trustProxySetting } from './proxy-config';
describe('trust proxy configuration',()=>{it('defaults to disabled',()=>expect(trustProxySetting({})).toBe(false));it('accepts a fixed hop count or explicit CIDRs',()=>{expect(trustProxySetting({TRUST_PROXY_HOPS:'1'})).toBe(1);expect(trustProxySetting({TRUST_PROXY_CIDRS:'10.0.0.0/8,192.0.2.1'})).toEqual(['10.0.0.0/8','192.0.2.1'])});it.each([{TRUST_PROXY_HOPS:'all'},{TRUST_PROXY_HOPS:'1',TRUST_PROXY_CIDRS:'10.0.0.0/8'},{TRUST_PROXY_CIDRS:'*'}])('rejects unsafe proxy config %j',(env)=>expect(()=>trustProxySetting(env)).toThrow())});
describe('client IP normalization',()=>{it('normalizes IPv4-mapped and IPv6 text',()=>{expect(normalizeClientIp('::FFFF:192.0.2.4')).toBe('192.0.2.4');expect(normalizeClientIp(' 2001:DB8::1 ')).toBe('2001:db8::1')})});
