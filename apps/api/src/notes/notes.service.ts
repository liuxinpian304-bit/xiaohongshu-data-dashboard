import { Injectable,NotFoundException } from '@nestjs/common'; import { prisma } from '@xhs/database'; import { page } from '../common/pagination.dto';
@Injectable() export class NotesService { async list(accountId:string|undefined,cursor:string|undefined,limit:number){return page(await prisma.note.findMany({where:{...(accountId?{accountId}:{}),...(cursor?{id:{gt:cursor}}:{})},orderBy:{id:'asc'},take:limit+1}),limit)} async detail(id:string){const note=await prisma.note.findUnique({where:{id}});if(!note)throw new NotFoundException('note not found');const snapshots=await prisma.metricSnapshot.findMany({where:{noteId:id,supersededAt:null},orderBy:[{observedAt:'desc'},{revision:'desc'}],include:{metricDefinition:true}});const seen=new Set<string>();const metrics=snapshots.flatMap(s=>{const key=s.metricDefinition.key;if(seen.has(key))return[];seen.add(key);return[{key,displayName:s.metricDefinition.displayName,availability:s.availability,value:s.value?.toString()??null,source:s.source,observedAt:s.observedAt.toISOString()}]});return{...note,metrics}} }
