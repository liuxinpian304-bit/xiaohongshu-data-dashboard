import{Injectable}from'@nestjs/common';import{prisma}from'@xhs/database';import{page}from'../common/pagination.dto';@Injectable()export class ReportsService{async list(accountId:string|undefined,cursor:string|undefined,limit:number){return page(await prisma.report.findMany({where:{...(accountId?{accountId}:{}),...(cursor?{id:{gt:cursor}}:{})},orderBy:{id:'asc'},take:limit+1,include:{metrics:true}}),limit)}}
