import { notFound, redirect } from 'next/navigation'; import { getComments, getNotes } from '../../../../lib/api'; import { formatShanghaiDateTime } from '../../../../lib/format'; import { CommentTree } from '../../../../components/comment-tree';
export default async function NoteDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const notes = await getNotes(); if (notes.status === 'unauthorized') redirect(`/login?next=/notes/${id}`); if (notes.status === 'error') return <div className="workflow-page"><section className="load-error" role="alert"><h2>笔记暂时无法加载</h2><a href={`/notes/${id}`}>重新加载</a></section></div>; const note = notes.data.items.find((item) => item.id === id); if (!note) notFound(); const comments = await getComments({ noteId: id }); return <div className="workflow-page"><header className="workflow-heading"><div><a href="/notes">← 返回笔记</a><h1>{note.title}</h1><p>发布于 {formatShanghaiDateTime(note.publishedAt)}</p></div></header><section className="metric-placeholder"><h2>笔记指标</h2><div><strong>浏览</strong><span>待导入</span></div><div><strong>点赞</strong><span>待导入</span></div><div><strong>评论</strong><span>待导入</span></div><p>当前 API 未返回该笔记的 MetricSnapshot，不会用0代替未知值。</p></section><section className="panel"><div className="panel-heading"><div><h2>最新评论</h2><p>仅展示当前加载页</p></div></div>{comments.status === 'ok' ? <CommentTree comments={comments.data.items} completeness={comments.data.pageInfo.hasMore ? 'has_more' : 'page_complete'} /> : <div className="compact-error">评论暂时无法加载。</div>}</section></div>; }
