import { forwardMutation } from '../../../../lib/bff'; const valid=(id:string)=>/^[0-9a-f-]{36}$/i.test(id); export async function DELETE(request: Request, context:{params:Promise<{id:string}>}) { const {id}=await context.params; return valid(id)?forwardMutation(request,`/accounts/${id}`,'DELETE'):new Response(null,{status:400}); }
