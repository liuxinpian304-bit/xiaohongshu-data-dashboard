export async function clientMutation(url:string,method:string,body:unknown){const response=await fetch(url,{method,headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(response.status===401){location.assign('/login?reason=session-expired');return{response,message:'登录已失效，正在返回登录页…'}}if(response.status===403)return{response,message:'没有权限完成此操作。'};return{response,message:response.ok?'操作已完成':'操作失败，请重试。'}}
