{
  "name": "worker",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests --no-file-parallelism",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/web-push": "^3.6.4",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@xhs/connector": "workspace:*",
    "@xhs/database": "workspace:*",
    "@xhs/domain": "workspace:*",
    "@xhs/self-scrape-import": "workspace:*",
    "bullmq": "^6.0.5",
    "date-fns-tz": "^3.2.0",
    "ioredis": "^6.0.0",
    "web-push": "^3.6.7"
  }
}
