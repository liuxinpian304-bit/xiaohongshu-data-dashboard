# Task 9A Round 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close dashboard segment, sync identity, revocation lifecycle, and production database-role gaps.

**Architecture:** Preserve full effective-dated provenance through aggregation, validate observations against their selected immutable definition, split remote revocation across two short locked transactions with persisted retry state, and make production database configuration fail closed.

**Tech Stack:** TypeScript, NestJS, Prisma 7, PostgreSQL 18, Vitest, Docker Compose.

## Global Constraints

- Do not modify or commit the pre-existing Task 5 report change.
- Use forward migrations only and preserve migration 0015's security-definer correction function.
- Run database-mutating integration suites sequentially.

---

### Task 1: Dashboard definition segments

**Files:** Modify `apps/api/src/dashboard/dashboard.service.ts`; test `apps/api/src/dashboard/dashboard.service.spec.ts`.

- [ ] Add tests proving a complete compatible transition gives identical final card/trend values and a missing boundary makes both `not_synced`.
- [ ] Run the focused test and confirm failure caused by key-compressed definitions.
- [ ] Keep complete definition segments for `seriesDeltas`; derive only a unique presentation-key list.
- [ ] Run the focused test and confirm it passes.

### Task 2: Sync observation identity

**Files:** Modify `apps/worker/src/sync/sync.repository.ts`; test `apps/worker/src/sync/sync.service.spec.ts`.

- [ ] Add a same-aggregation, different-version batch test that must reject without evidence writes.
- [ ] Run it and confirm the current repository incorrectly accepts the observation.
- [ ] Require metadata key/source/aggregation/version to equal the selected definition.
- [ ] Run the sync tests and confirm they pass.

### Task 3: Revocation state machine

**Files:** Modify `packages/database/prisma/schema.prisma`, add migration `0016_account_revocation_state`, modify `apps/api/src/accounts/accounts.service.ts`, and extend its integration spec.

- [ ] Add tests for disabled pending state before remote I/O, failure preservation, successful retry, repeated deletion, concurrent history insertion, and remote-success/finalize-failure recovery.
- [ ] Run tests and confirm the single-transaction implementation fails.
- [ ] Add persisted revocation state/intent and implement short prepare/finalize transactions around the remote call.
- [ ] Generate the Prisma client, migrate the test database, and run account integration tests.

### Task 4: Fail-closed database role

**Files:** Modify `packages/database/src/client.ts`, API/worker startup tests or database unit tests, `.env.example`, `docker-compose.yml`, runtime-role init script, and `docs/database-deployment.md`.

- [ ] Add behavioral tests for missing URL, owner role, missing opt-out boolean, valid runtime role, and the exact test-only bypass.
- [ ] Run tests and confirm permissive defaults fail them.
- [ ] Remove the owner fallback, verify by default, and add the two-variable test bypass.
- [ ] Replace the fixed password with a required Compose secret and safely quoted psql provisioning.
- [ ] Run focused tests and exercise the init script with punctuation-bearing passwords.

### Task 5: Migration and final verification

**Files:** Append round 3 evidence to the ignored Task 9A report.

- [ ] Apply every migration to a fresh database and apply forward migrations to a representative pre-round3 database.
- [ ] Verify the runtime role cannot update, delete, or truncate metric evidence and can invoke the correction function.
- [ ] Run domain, connector, web, API, worker, and database tests sequentially.
- [ ] Run every workspace typecheck and production build.
- [ ] Run `git diff --check`, stage only Task 9A files, commit, and record the SHA in the report.
